// import { setCartSelectItems, clearCartSelect } from '../slices/cartSlice';

// export const saveSelectedCartItems = (selectedItems) => (dispatch) => {
//     dispatch(setCartSelectItems(selectedItems));
// };

// export const resetCartSelection = () => (dispatch) => {
//     dispatch(clearCartSelect());
// };